
check mosquitto with:

```shell
sudo systemctl restart mosquitto
sudo systemctl status mosquitto

sudo nano /etc/mosquitto/mosquitto.conf
```

Remember to replace IPs adresses in:

- mosquitto conf
- rasp_server
- pc_client

run rasp server with:

```
sudo ~/ProgTank/tankenv/bin/python3 rasp_server.py
```

gst-launch-1.0 -v libcamerasrc ! video/x-raw,width=640,height=480,framerate=30/1 ! \
videoconvert ! x264enc tune=zerolatency bitrate=1000000 speed-preset=ultrafast ! \
h264parse ! rtph264pay config-interval=1 pt=96 ! udpsink host=<PC_IP> port=5000


gst-launch-1.0 -v udpsrc port=5000 caps="application/x-rtp,media=video,encoding-name=H264,payload=96" ! \
rtph264depay ! h264parse ! avdec_h264 ! videoconvert ! autovideosink

